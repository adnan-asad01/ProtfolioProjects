SELECT Gender
FROM EmployeeDemo
GROUP BY Gender

SELECT Gender, Count(Gender)
FROM EmployeeDemo
GROUP BY Gender


SELECT *
FROM EmployeeDemo
ORDER BY Age

SELECT *
FROM EmployeeDemo
ORDER BY 4 Desc, 5 Desc

